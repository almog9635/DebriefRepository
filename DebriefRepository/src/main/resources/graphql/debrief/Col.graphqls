type Col implements OrderItem {
    id: ID!
    index: Int!
    name: String!
    table: Table!
    cells: [Cell!]!
}

input ColInput{
    id: ID
    index: Int
    name: String
    table: ID
    cell: [CellInput!]
}