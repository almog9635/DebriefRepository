type Table implements ContentItem & OrderedItem {
    id: ID!
    index: Int!
    name: String!
    rows: [Row!]!
    columns: [Col!]!
}

input TableInput {
    id: ID
    index: Int
    name: String
    rows: [RowInput!]
    columns: [ColInput!]
}

