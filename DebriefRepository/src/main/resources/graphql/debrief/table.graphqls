type Table implements OrderItem {
    id: ID!
    index: Int!
    rows: [Row!]!
    columns: [Column!]!
}

input TableInput {
    id: ID
    index: Int
    rows: [RowInput!]
    columns: [ColumnInput!]
}

