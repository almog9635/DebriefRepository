type Table implements ContentItem & OrderItem {
    id: ID!
    index: Int!
    name: String!
    rows: [Row!]!
    columns: [Col!]!
}

input TableInput {
    id: ID
    index: Int
    name: String
    rows: [RowInput!]
    columns: [ColInput!]
}

